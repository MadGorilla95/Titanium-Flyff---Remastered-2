<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:Forms="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms" mc:Ignorable="d" x:Class="PatchClient.Options"
        Title="Options" Height="270" Width="454" Background="{x:Null}" OpacityMask="White" AllowsTransparency="True" WindowStyle="None" HorizontalAlignment="Center" VerticalAlignment="Center" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False" >
    
    <Window.Resources>
        <Style TargetType="{x:Type Image}">
            <Setter Property="Opacity" Value="1.0" />
        </Style>

        <!--
        ComboBox style
        -->
        <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="{x:Type ToggleButton}">
            <Grid Width="120" Height="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="20" />
                </Grid.ColumnDefinitions>
                <Image x:Name="Border"  Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Center" VerticalAlignment="Center" Source="Images/combobox_image.png" Width="131" Height="33" Stretch="None"></Image>

                <Image x:Name="Arrow"  Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Center" Source="Images/combobox_arrow.png" Width="12" Height="11" Stretch="None"></Image>
            </Grid>
        </ControlTemplate>

        <ControlTemplate x:Key="ComboBoxTextBox" TargetType="{x:Type TextBox}">
            <Border x:Name="PART_ContentHost" Focusable="False" Background="{TemplateBinding Background}" />
        </ControlTemplate>

        <Style x:Key="{x:Type ComboBox}" TargetType="{x:Type ComboBox}">
            <Setter Property="SnapsToDevicePixels" Value="true"/>
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="true"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="MinHeight" Value="20"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="MaxWidth" Value="120"/>
            <Setter Property="MaxHeight" Value="20"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Template">
                
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid>
                            <ToggleButton 
                            Name="ToggleButton" 
                            Template="{StaticResource ComboBoxToggleButton}" 
                            Grid.Column="2" 
                            Focusable="false"
                            IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}"
                            ClickMode="Press">
                            </ToggleButton>
                            <ContentPresenter Name="ContentSite" IsHitTestVisible="False"  Content="{TemplateBinding SelectionBoxItem}"
                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                            ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                            Margin="5,3,23,3"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Left" />
                            <TextBox x:Name="PART_EditableTextBox"
                            Style="{x:Null}" 
                            Template="{StaticResource ComboBoxTextBox}" 
                            HorizontalAlignment="Left" 
                            VerticalAlignment="Center" 
                            Margin="3,3,23,3"
                            Focusable="True" 
                            Background="red"
                            Foreground="Green"
                            Visibility="Hidden"
                            IsReadOnly="{TemplateBinding IsReadOnly}"/>
                            <Popup 
                            Name="Popup"
                            Placement="Bottom"
                            IsOpen="{TemplateBinding IsDropDownOpen}"
                            AllowsTransparency="True" 
                            Focusable="False"
                            PopupAnimation="Slide">

                                <Grid Name="DropDown"
                              SnapsToDevicePixels="True"                
                              MinWidth="{TemplateBinding ActualWidth}"
                              MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border
                                x:Name="DropDownBorder"
                                Background="black"

                                BorderThickness="1"
                                BorderBrush="#816c45"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True" HorizontalAlignment="Left">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="false">
                                <Setter TargetName="DropDownBorder" Property="MinHeight" Value="95"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="Gray"/>
                            </Trigger>
                            <Trigger Property="IsGrouping" Value="true">
                                <Setter Property="ScrollViewer.CanContentScroll" Value="false"/>
                            </Trigger>
                            <Trigger SourceName="Popup" Property="Popup.AllowsTransparency" Value="true">
                                <Setter TargetName="DropDownBorder" Property="CornerRadius" Value="0"/>
                                <Setter TargetName="DropDownBorder" Property="Margin" Value="0,2,0,0"/>
                            </Trigger>
                            <Trigger Property="IsEditable"  Value="true">
                                <Setter Property="IsTabStop" Value="false"/>
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
            </Style.Triggers>
        </Style>

        <!-- SimpleStyles: ComboBoxItem -->
        <Style x:Key="{x:Type ComboBoxItem}" TargetType="{x:Type ComboBoxItem}">
            <Setter Property="SnapsToDevicePixels" Value="true"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                        <Border Name="Border"
                              Padding="2"
                              SnapsToDevicePixels="true">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="true">
                                <Setter TargetName="Border" Property="Background" Value="#FF4F4F4F"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="#888888"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!--
        /  ComboBox style
        -->

        <!--
        CheckBox style
        -->
        <Style x:Key="{x:Type CheckBox}"
       TargetType="{x:Type CheckBox}">
            <Setter Property="SnapsToDevicePixels"
          Value="true" />
            <Setter Property="OverridesDefaultStyle"
          Value="true" />
            <Setter Property="FocusVisualStyle"
          Value="{DynamicResource CheckBoxFocusVisual}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type CheckBox}">
                        <BulletDecorator Background="Transparent">
                            <BulletDecorator.Bullet>
                                <Grid Width="18" Height="18">

                                    <Rectangle x:Name="Border"
                       StrokeThickness="1">
                                        <Rectangle.Fill>
                                            <ImageBrush ImageSource="Images/checkbox_button_empty.png" Stretch="Fill"/>
                                        </Rectangle.Fill>

                                    </Rectangle>
                                    <Rectangle x:Name="CheckMark"
                       Margin="4"
                       Visibility="Collapsed">
                                        <Rectangle.Fill>
                                            <ImageBrush ImageSource="Images/checkbox_button_selected.png" Stretch="Fill"/>
                                        </Rectangle.Fill>
                                    </Rectangle>
                                </Grid>
                            </BulletDecorator.Bullet>
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="MouseOver"/>
                                    <VisualState x:Name="Pressed"/>
                                    <VisualState x:Name="Disabled"/>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="CheckStates">
                                    <VisualState x:Name="Checked">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Visibility)"
                                                 Storyboard.TargetName="CheckMark">
                                                <DiscreteObjectKeyFrame KeyTime="0"
                                            Value="{x:Static Visibility.Visible}" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Unchecked" />
                                    <VisualState x:Name="Indeterminate" />
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <ContentPresenter Margin="4,0,0,0"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Left"
                            RecognizesAccessKey="True" />
                        </BulletDecorator>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

        </Style>
        <!--
        /  CheckBox style
        -->
        <!--
        Radiobutton style
        -->

        
        <Style x:Key="{x:Type RadioButton}"
       TargetType="{x:Type RadioButton}">
            <Setter Property="SnapsToDevicePixels"
          Value="true" />
            <Setter Property="OverridesDefaultStyle"
          Value="true" />
            <Setter Property="FocusVisualStyle"
          Value="{DynamicResource RadioButtonFocusVisual}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RadioButton}">
                        <BulletDecorator Background="Transparent">
                            <BulletDecorator.Bullet>
                                <Grid Width="29"
                  Height="29">

                                    <Ellipse x:Name="Border"
                       StrokeThickness="1">
                                        <Ellipse.Fill>
                                            <ImageBrush ImageSource="Images/radio_button_empty.png" Stretch="None"/>
                                        </Ellipse.Fill>
       
                                    </Ellipse>
                                    <Ellipse x:Name="CheckMark"
                       Margin="4"
                       Visibility="Collapsed">
                                        <Ellipse.Fill>
                                            <ImageBrush ImageSource="Images/radio_button_selected.png" Stretch="None"/>
                                        </Ellipse.Fill>
                                    </Ellipse>
                                </Grid>
                            </BulletDecorator.Bullet>
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Normal" />
                                    <VisualState x:Name="MouseOver"/>
                                    <VisualState x:Name="Pressed"/>
                                    <VisualState x:Name="Disabled"/>
                                </VisualStateGroup>
                                <VisualStateGroup x:Name="CheckStates">
                                    <VisualState x:Name="Checked">
                                        <Storyboard>
                                            <ObjectAnimationUsingKeyFrames Storyboard.TargetProperty="(UIElement.Visibility)"
                                                 Storyboard.TargetName="CheckMark">
                                                <DiscreteObjectKeyFrame KeyTime="0"
                                            Value="{x:Static Visibility.Visible}" />
                                            </ObjectAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                    <VisualState x:Name="Unchecked" />
                                    <VisualState x:Name="Indeterminate" />
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <ContentPresenter Margin="4,0,0,0"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Left"
                            RecognizesAccessKey="True" />
                        </BulletDecorator>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!--
        / Radiobutton style
        -->
        <Style TargetType="{x:Type Button}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Name="Border" BorderThickness="0"
                    BorderBrush="Transparent">
                            <ContentPresenter
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="Border" 
                          Property="Background" Value="Transparent" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter TargetName="Border" 
                          Property="BorderThickness" Value="1" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Image x:Key="saveHover" Source="Images/save_01.png" />
        <Image x:Key="saveNormal" Source="Images/save_00.png"/>
        <Image x:Key="cancelHover" Source="Images/cancel_01.png" />
        <Image x:Key="cancelNormal" Source="Images/cancel_00.png"/>
    </Window.Resources>
    <Grid>
        <Grid.Background >
            <ImageBrush ImageSource="Images/Options.png"></ImageBrush>
        </Grid.Background>
        <RadioButton Name="RangeFar" Tag="0" GroupName="Range" Content="" HorizontalAlignment="Left" Margin="38,179,0,0" VerticalAlignment="Top" Background="Green" Width="29" Height="29" BorderBrush="Red" Foreground="Yellow" />
        <RadioButton Name="RangeMid" Tag="1" GroupName="Range" Content="" HorizontalAlignment="Left" Margin="87,179,0,0" VerticalAlignment="Top" Background="Green" Width="29" Height="29"  BorderBrush="Red" Foreground="Yellow"/>
        <RadioButton Name="RangeLow"  Tag="2" GroupName="Range" Content="" HorizontalAlignment="Left" Margin="153,179,0,0" VerticalAlignment="Top" Background="White" Width="29" Height="29"/>

        <RadioButton Name="TextureHigh" Tag="0" GroupName="Texture" Content="" HorizontalAlignment="Left" Margin="248,179,0,0" VerticalAlignment="Top" Background="White" Width="29" Height="29"/>
        <RadioButton Name="TextureMid" Tag="1" GroupName="Texture" Content="" HorizontalAlignment="Left" Margin="298,179,0,0" VerticalAlignment="Top" Background="White" Width="29" Height="29"/>
        <RadioButton Name="TextureLow"  Tag="2" GroupName="Texture" Content="" HorizontalAlignment="Left" Margin="364,179,0,0" VerticalAlignment="Top" Background="White" Width="29" Height="29"/>

        <RadioButton Name="ObjectHigh" Tag="0" GroupName="Object" Content="" HorizontalAlignment="Left" Margin="248,229,0,0" VerticalAlignment="Top" Background="White" Width="29" Height="29"/>
        <RadioButton Name="ObjectMid" Tag="1" GroupName="Object" Content="" HorizontalAlignment="Left" Margin="298,229,0,0" VerticalAlignment="Top" Background="White" Width="29" Height="29"/>
        <RadioButton Name="ObjectLow"  Tag="2" GroupName="Object" Content="" HorizontalAlignment="Left" Margin="364,229,0,0" VerticalAlignment="Top" Background="White" Width="29" Height="29" RenderTransformOrigin="0.5,0.5"/>

        <RadioButton Name="ShadowHigh" Tag="0" GroupName="Shadow" Content="" HorizontalAlignment="Left" Margin="38,229,0,0" VerticalAlignment="Top" Background="White" Width="29" Height="29"/>
        <RadioButton Name="ShadowMid" Tag="1" GroupName="Shadow" Content="" HorizontalAlignment="Left" Margin="87,229,0,0" VerticalAlignment="Top" Background="White" Width="29" Height="29"/>
        <RadioButton Name="ShadowLow"  Tag="2" GroupName="Shadow" Content="" HorizontalAlignment="Left" Margin="153,229,0,0" VerticalAlignment="Top" Background="White" Width="29" Height="29"/>


        <Button x:Name="ButtonSave" HorizontalAlignment="Left" Margin="335,24,0,0" VerticalAlignment="Top" Width="80" Height="19" BorderBrush="Transparent" Cursor="Hand" Click="ButtonSave_Click" MouseEnter="ButtonSave_MouseEnter" MouseLeave="ButtonSave_MouseLeave">
            <Image Source="Images/save_00.png"></Image>
        </Button>



        <Button x:Name="ButtonCancel" HorizontalAlignment="Left" Margin="335,51,0,0" VerticalAlignment="Top" Width="80" Height="19" BorderBrush="Transparent" Cursor="Hand" Click="ButtonCancel_Click" MouseEnter="ButtonCancel_MouseEnter" MouseLeave="ButtonCancel_MouseLeave">
            <Image Source="Images/cancel_00.png"></Image>
        </Button>


        <ComboBox x:Name="Resolution" HorizontalAlignment="Left" Height="33" Margin="42,115,0,0" VerticalAlignment="Top" Width="131" Padding="6,0,5,0"/>

        <CheckBox Name="FullScreen" HorizontalAlignment="Left" Margin="44,137,0,0" VerticalAlignment="Top" TextOptions.TextFormattingMode="Display"/>

        <CheckBox Name="start_32bit_filter_chkbox" HorizontalAlignment="Left" Margin="254,85,0,0" VerticalAlignment="Top" TextOptions.TextFormattingMode="Display"/>
        <CheckBox Name="Ani_filter_chkbox" HorizontalAlignment="Left" Margin="254,103,0,0" VerticalAlignment="Top" TextOptions.TextFormattingMode="Display"/>
        <CheckBox Name="antialiasing_chkbox" HorizontalAlignment="Left" Margin="254,121,0,0" VerticalAlignment="Top" TextOptions.TextFormattingMode="Display"/>
        <CheckBox Name="low_render_chkbox" HorizontalAlignment="Left" Margin="254,140,0,0" VerticalAlignment="Top" TextOptions.TextFormattingMode="Display"/>

    </Grid>
</Window>
